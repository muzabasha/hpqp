const stories = {
  1: 'Imagine a school cafeteria at lunchtime. One cook serves one hundred students: the first plates move quickly, then the queue grows, tempers rise, and everyone starts asking for a faster kitchen. The principal buys four more stoves, but nobody decides who uses which stove. Now there are five cooks arguing over one spoon. A thoughtful student asks: what is the queue, what can be shared, and what should we measure?',
  2: 'Imagine four friends sorting a huge box of buttons. At first they work together, but two friends grab the same button, one friend finishes early, and another keeps walking back to the box. They discover that rules for ownership and communication matter as much as the number of hands. The group becomes faster only after they agree on a plan.',
  3: 'Imagine a very fast courier team delivering parcels from a warehouse. The couriers are quick, but the warehouse shelves are badly arranged. Every courier waits while searching, and adding more couriers only creates a traffic jam. A small redesign of shelf layout makes the whole team feel magically faster.',
  4: 'Imagine a pair of magical coins. Each coin seems undecided until someone looks, yet the two coins can still reveal matching answers even when separated. Your friend claims the coins are simply hiding instructions. You design a careful experiment: prepare them, change them, measure them, and repeat the trial many times.'
};
const models = {
  1: { equation: 'S(p) = 1 / (s + (1 - s) / p)', symbols: 'S(p) is speedup, s is the serial fraction, and p is the processor count.', example: 'With s = 0.10 and p = 16, S = 5.93 and efficiency E = S/p = 37.1%.', metric: 'speedup, efficiency, latency, bandwidth, and energy per useful result' },
  2: { equation: 'T(n) = T_serial + T_parallel/n + T_overhead(n)', symbols: 'T is total time, n is workers, and T_overhead is coordination and communication cost.', example: 'If serial work is 0.20, parallel work is 0.80, n = 4, and overhead is 0.02, relative time is 0.42.', metric: 'runtime, throughput, communication volume, imbalance, and synchronization wait' },
  3: { equation: 'P = min(P_compute, P_memory)', symbols: 'P is achieved performance; P_compute is arithmetic capacity; P_memory is data-delivery capacity.', example: 'A kernel with 80 units of compute capacity and 45 units of memory capacity reaches only P = 45 units.', metric: 'occupancy, coalescing, bandwidth, achieved FLOP/s, and kernel time' },
  4: { equation: '|ψ⟩ = α|0⟩ + β|1⟩,  |α|² + |β|² = 1', symbols: 'ψ is the qubit state, α and β are complex amplitudes, and |α|² and |β|² are measurement probabilities.', example: 'For α = β = 1/√2, measuring many times approaches 50% zero and 50% one.', metric: 'fidelity, probability distribution, circuit depth, gate count, and objective value' }
};

const list = (items) => `<ul>${items.map((item) => `<li>${item}</li>`).join('')}</ul>`;
const activity = (level, title, objective, instructions, input, output, time) => `<article class="activity-card"><div class="eyebrow">Level ${level} · ${title}</div><h3>${objective}</h3><p><strong>Instructions:</strong> ${instructions}</p><p><strong>Inputs:</strong> ${input} · <strong>Expected output:</strong> ${output}</p><p><strong>Assessment:</strong> accuracy 40%, reasoning 30%, collaboration or reflection 30% · <strong>Time:</strong> ${time} · <strong>Materials:</strong> board, browser lab, worksheet.</p></article>`;

export function renderTopicLesson({ unitId, topicId, title, description, activityBrief, nextTopic }) {
  const story = stories[unitId];
  const model = models[unitId];
  const dependency = unitId === 1 ? 'The next Unit 1 ideas depend on this: memory, performance laws, and energy-aware cluster choices.' : unitId === 2 ? 'Later parallel topics depend on this: MPI communication, synchronization, load balancing, and multiprocessing.' : unitId === 3 ? 'Later GPU topics depend on this: memory behavior, profiling, cloud deployment, and AI accelerators.' : 'Later quantum topics depend on this: entanglement, algorithms, VQE, QAOA, and hybrid design.';
  return `<div class="lesson-header"><div class="eyebrow">Unit ${unitId} / Topic ${topicId} / Complete lesson</div><h1>${title}</h1><p class="lede">${description}</p></div><div class="lesson-nav"><a class="button" href="#/unit/${unitId}">← Unit map</a><span>Topic ${topicId}</span><a class="button" href="#/unit/${unitId}/topic/${nextTopic}">Next topic →</a></div>
  <section class="lesson-section"><div class="section-heading"><div><div class="eyebrow">Section 1</div><h2>Prepare the ground</h2></div><p>Prerequisite → dependency → next step</p></div>${list([`Prerequisites for this topic: ${unitId === 1 ? 'basic algorithms, computer organization, and linear algebra' : unitId === 2 ? 'Unit 1 performance vocabulary and C/Python loops' : unitId === 3 ? 'thread/process concepts and memory hierarchy' : 'probability, vectors, and basic matrix notation'}.`, dependency, `Prepare for the next topic (${nextTopic}): review today’s vocabulary, complete the lab reflection, and write one question about ${nextTopic}.`])}</section>
  <section class="lesson-section story-section"><div class="section-heading"><div><div class="eyebrow">Section 2</div><h2>A story before the terminology</h2></div><p>Remember the idea before the label.</p></div><p class="story-lead">${story}</p>${list(['What was the queue, uncertainty, or hidden cost in the story?', 'What would you measure before buying more equipment?', 'Which rule made the group more reliable?', 'What did we just learn?'])}<p class="callout"><strong>What did we just learn?</strong> A system improves when we identify the work, the constraints, the communication, and the evidence. Now the story connects to <strong>${title}</strong>.</p><details open><summary>Technical bridge</summary><p>${description} Start with the simple model, name each moving part, test it with a small input, and only then introduce the deeper terminology. This lowers fear, improves recall, and trains critical thinking.</p></details><p>Mastery check: explain the idea to a classmate using the story, then replace each story object with its technical counterpart.</p></section>
  <section class="lesson-section"><div class="section-heading"><div><div class="eyebrow">Section 3</div><h2>Mathematical modelling</h2></div><p>Equation → simulation → decision</p></div><div class="model-grid"><div><h3>Why model it?</h3>${list(['Need: predict behavior before spending compute time or hardware budget.', 'Motivation: turn an intuition into a testable relationship.', `Real-world challenge: ${unitId === 1 ? 'bigger machines expose serial work and memory limits' : unitId === 2 ? 'coordination can erase the benefit of more workers' : unitId === 3 ? 'fast arithmetic can wait on slow data movement' : 'probability and noise make single runs misleading'}.`, `Technical challenge: choose a useful abstraction without hiding the bottleneck.`, `Advantages: comparison, sensitivity analysis, reproducibility, and optimization guidance.`, 'Limitations: synthetic inputs omit some hardware, noise, and workload behavior.'])}</div><div class="equation-card"><div class="equation">\\(${model.equation}\\)</div><p><strong>Symbols:</strong> ${model.symbols}</p><p><strong>Meaning:</strong> Each term represents a measurable part of the workload. The equation is needed to compare alternatives and interpret a simulation rather than guess.</p><p><strong>Numerical example:</strong> ${model.example}</p><p><strong>Evaluation:</strong> Track ${model.metric}. Discuss whether the result is limited by the model or by the implementation.</p></div></div><div class="model-controls"><div class="control"><label for="model-input">Synthetic input <output data-model-input-value>50</output></label><input id="model-input" data-model-input type="range" min="10" max="100" value="50" /></div><div class="control"><label for="model-sensitivity">Sensitivity <output data-model-sensitivity-value>50%</output></label><input id="model-sensitivity" data-model-sensitivity type="range" min="0" max="100" value="50" /></div></div><div class="model-chart" aria-label="Dynamic model comparison"><i style="height:50%" data-model-bar="input"></i><i style="height:50%" data-model-bar="baseline"></i><i style="height:50%" data-model-bar="tuned"></i></div><div class="chart-labels"><span>input</span><span>baseline</span><span>tuned</span></div><p class="callout" data-model-feedback>Change a slider to see the synthetic output, baseline comparison, and sensitivity response update in real time. Optimization starts by reducing the dominant term; future enhancements can replace the synthetic model with measured hardware traces.</p></section>
  <section class="lesson-section"><div class="section-heading"><div><div class="eyebrow">Section 4</div><h2>Activity-based learning</h2></div><p>Four levels, one progression.</p></div>${activity(1, 'Teacher Do', 'See the concept performed safely.', 'Demonstrate the model with one small input, narrate each step, and pause before revealing the result.', 'prepared example and projector', 'shared observation and vocabulary', '10 min')}${activity(2, 'Teacher + Student Together', 'Connect the model to evidence.', 'Pairs predict the result, change one parameter with the instructor, and defend the prediction to a partner.', 'worksheet and guided lab', 'annotated comparison table', '15 min')}${activity(3, 'All Students Do', 'Experiment collaboratively.', 'Teams run two trials, assign observer, operator, analyst, and reporter roles, then compare explanations across teams.', 'synthetic workload and lab controls', 'team graph plus claim-evidence-reasoning statement', '25 min')}${activity(4, 'Individual Student Do', 'Transfer the idea independently.', 'Solve one numerical case, run one changed-parameter trial, and self-evaluate using the rubric.', 'individual worksheet and browser', 'worked answer and reflection paragraph', '20 min')}</section>
  <section class="lesson-section"><div class="section-heading"><div><div class="eyebrow">Section 5</div><h2>Project-based learning</h2></div><p>From classroom model to TRL 3 demonstration.</p></div><div class="project-grid"><div>${list([`Scope: build a small ${title.toLowerCase()} demonstrator using synthetic or measured input.`, 'Feasibility: browser simulation first; optional Python/CUDA/Qiskit implementation second.', 'Objectives: measure a baseline, change one design variable, explain the trade-off, and document reproducibility.', 'Methodology: question → hypothesis → experiment → comparison → reflection.', 'Outcomes: a runnable prototype, evidence chart, user manual, and five-minute demonstration.'])}</div><div>${list(['Risk heatmap: incorrect assumptions (high/medium), unavailable hardware (medium/low), unclear roles (medium/medium), noisy measurements (high/medium).', 'Budget estimate: student time 12 hours; cloud or lab allowance $0–$25; documentation and review 3 hours.', 'Timeline: Day 1 scope; Days 2–3 baseline; Days 4–5 optimization; Day 6 validation; Day 7 showcase.', 'Team roles: systems lead, experiment lead, data analyst, QA/documentation lead.', 'TRL 3: analytical and experimental proof of concept in a representative simulation.'])}</div></div><div class="timeline" aria-label="Project timeline"><i style="width:14%">scope</i><i style="width:28%">baseline</i><i style="width:28%">optimize</i><i style="width:30%">validate</i></div><table class="project-table"><thead><tr><th>Milestone</th><th>Deliverable</th><th>Dependency</th><th>Owner</th></tr></thead><tbody><tr><td>Baseline</td><td>Input/output record</td><td>Prerequisite review</td><td>Experiment lead</td></tr><tr><td>Comparison</td><td>Chart and risk note</td><td>Working lab</td><td>Data analyst</td></tr><tr><td>TRL 3 demo</td><td>Prototype and user manual</td><td>QA pass</td><td>Whole team</td></tr></tbody></table><p><strong>User manual:</strong> open the topic, reset the lab, set one input, press or move one control, record the metric, compare with baseline, and explain one improvement and one limitation.</p></section>
  <section class="lesson-section"><div class="section-heading"><div><div class="eyebrow">Section 6</div><h2>Model 2-mark questions</h2></div><p>Recall, calculate, apply, solve.</p></div><div class="question-grid"><article class="card"><h3>Conceptual</h3><p>What is the central idea in ${title}?</p><p><strong>Answer:</strong> It is a measurable way to understand ${description.toLowerCase()}.</p><p class="callout">Discuss the definition, avoid the common mistake of naming a tool without explaining the mechanism, and remember: mechanism before brand.</p></article><article class="card"><h3>Numerical</h3><p>Use the model with the example values shown above. What does the result mean?</p><p><strong>Answer:</strong> Substitute each symbol, keep units consistent, and compare the result with the baseline.</p><p class="callout">Show substitutions step by step; do not report a number without interpretation.</p></article><article class="card"><h3>Application</h3><p>When would this idea change an engineering decision?</p><p><strong>Answer:</strong> When the measured bottleneck or constraint differs from the assumed one.</p><p class="callout">Remember: observe, explain, then optimize.</p></article><article class="card"><h3>Problem solving</h3><p>Give one improvement and one limitation for the model.</p><p><strong>Answer:</strong> Improve it with measured traces; limit it by testing more workloads and hardware conditions.</p><p class="callout">A strong answer includes evidence, a trade-off, and a next experiment.</p></article></div></section>
  <section class="lesson-section"><div class="section-heading"><div><div class="eyebrow">Section 7 · NEP 2020</div><h2>Learn by doing</h2></div><p>Experiment → observe → analyze → compare → reflect → evaluate</p></div><p>Use the virtual laboratory below as a guided simulation. Reset before a new trial, change one parameter at a time, pause your reasoning before reading the feedback, and compare at least two runs. The controls expose synthetic inputs, dynamic outputs, data flow, step-by-step execution, and performance metrics.</p><div class="lab-steps"><span>1. Experiment</span><span>2. Observe</span><span>3. Analyze</span><span>4. Compare</span><span>5. Reflect</span><span>6. Evaluate</span></div></section>
  <section class="lesson-section end-section"><div class="section-heading"><div><div class="eyebrow">Topic synthesis</div><h2>Carry the idea forward</h2></div></div>${list([`Key insights: ${description}`, 'Advantages: measurable reasoning, reusable models, and stronger technical communication.', 'Disadvantages: abstraction can hide hardware details, cost, noise, or edge cases.', 'Possible improvements: use larger inputs, measured traces, alternate hardware, and peer review.', 'Future scope: connect this topic to the next topic and the Unit capstone.', `Industrial applications: ${unitId === 1 ? 'weather, seismic analysis, genomics, and energy systems' : unitId === 2 ? 'simulation services, search, finance, and data pipelines' : unitId === 3 ? 'AI inference, scientific imaging, robotics, and video analytics' : 'optimization research, chemistry, cryptography, and logistics'}.`, 'Research opportunities: reproducible benchmarking, energy-aware optimization, and hybrid algorithm design.', 'Career relevance: performance engineer, parallel programmer, GPU developer, quantum software engineer, systems researcher, and technical educator.'])}<button class="button primary" data-action="mark-topic" data-topic="${unitId}-${topicId}">Mark complete ✓</button></section>`;
}
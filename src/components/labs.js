export function renderLab(type) {
  if (type === 'parallel') return `<div class="lab-layout"><div class="lab-panel"><div class="eyebrow">OpenMP worksharing lab</div><h2>Parallelize the loop</h2><p class="lede">Change the number of workers and the share of independent work. The model includes thread overhead, so more workers are not automatically better.</p><div class="control"><label for="workers">Workers <output data-workers-value>4</output></label><input id="workers" data-workers type="range" min="1" max="16" value="4" /></div><div class="control"><label for="parallel-work">Parallel work <output data-parallel-work-value>80%</output></label><input id="parallel-work" data-parallel-work type="range" min="20" max="100" value="80" /></div><div class="formula">T = (1 - w) + w / n + overhead</div><div class="result"><div><strong data-parallel-time>0.27</strong><span>relative time</span></div><div><strong data-parallel-speedup>3.67×</strong><span>speedup</span></div><div><strong data-parallel-efficiency>91.8%</strong><span>efficiency</span></div></div></div><aside class="lab-panel"><div class="eyebrow">Code lens</div><h2>What changes?</h2><pre class="code-block"><code>#pragma omp parallel for\nfor (int i = 0; i &lt; n; i++)\n  output[i] = transform(input[i]);</code></pre><p class="callout" data-parallel-feedback>Independent iterations can share work. Shared writes still need a correctness argument.</p><button class="button primary" data-action="mark-lab" data-lab="parallel">Mark lab explored ✓</button></aside></div>`;
  if (type === 'gpu') return `<div class="lab-layout"><div class="lab-panel"><div class="eyebrow">GPU tuning lab</div><h2>Find the throughput sweet spot</h2><p class="lede">A simple model turns coalescing and occupancy into a relative kernel score. Tune both, then explain which constraint dominates.</p><div class="control"><label for="coalescing">Memory coalescing <output data-coalescing-value>70%</output></label><input id="coalescing" data-coalescing type="range" min="10" max="100" value="70" /></div><div class="control"><label for="occupancy">Occupancy <output data-occupancy-value>65%</output></label><input id="occupancy" data-occupancy type="range" min="10" max="100" value="65" /></div><div class="formula">Throughput ≈ coalescing × occupancy</div><div class="result"><div><strong data-gpu-score>45.5</strong><span>kernel score</span></div><div><strong data-gpu-bottleneck>memory</strong><span>likely limit</span></div><div><strong data-gpu-gain>+0%</strong><span>gain from baseline</span></div></div><div class="bar-chart" aria-label="GPU tuning score"><i data-gpu-bar style="height:45.5%"></i></div></div><aside class="lab-panel"><div class="eyebrow">Profiler note</div><h2>Read the signal</h2><p class="callout" data-gpu-feedback>Low coalescing means threads are asking memory for scattered addresses. Fix the access pattern before adding more blocks.</p><button class="button primary" data-action="mark-lab" data-lab="gpu">Mark lab explored ✓</button></aside></div>`;
  return `<div class="lab-layout"><div class="lab-panel"><div class="eyebrow">Quantum circuit lab</div><h2>Build a two-qubit state</h2><p class="lede">Apply gates to a small simulator. The simplified view reports the most likely measurement distribution after each operation.</p><div class="chip-row"><button class="button" data-gate="h">H · superpose</button><button class="button" data-gate="x">X · flip</button><button class="button" data-gate="cnot">CNOT · entangle</button><button class="button" data-gate="measure">Measure</button><button class="button" data-action="reset-circuit">Reset</button></div><div class="quantum-register"><div class="wire"><span>q₀</span><i data-wire-a></i></div><div class="wire"><span>q₁</span><i data-wire-b></i></div></div><div class="result"><div><strong data-state-label>|00⟩</strong><span>current state</span></div><div><strong data-state-probability>100%</strong><span>dominant probability</span></div><div><strong data-measurement>ready</strong><span>measurement</span></div></div></div><aside class="lab-panel"><div class="eyebrow">Reflect</div><h2>What did the gate do?</h2><p class="callout" data-quantum-feedback>Start with H to create superposition, then use CNOT to correlate the qubits.</p><button class="button primary" data-action="mark-lab" data-lab="quantum">Mark lab explored ✓</button></aside></div>`;
}
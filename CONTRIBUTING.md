# Contributing

Keep course content in `src/data`, route rendering in `src/core`, and user-facing interaction in `src/app.js` until the next component extraction phase. Run `npm run check` and `npm run build` before opening a pull request.